spring:
  application:
    name: operations-mcp-server
  ai:
    mcp:
      server:
        name: operations-mcp-server
        version: 0.1.0
        protocol: STREAMABLE
        capabilities:
          completion: false
          prompt: false
          resource: false

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
